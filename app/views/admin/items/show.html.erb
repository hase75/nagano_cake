<h1>商品詳細</h1>

<div>
  <%= image_tag @item.get_image %>
  <h4>商品名</h4>
  <%= @item.name %>
  <h4>商品説明</h4>
  <%= @item.introduction %>
  <h4>ジャンル</h4>
  <%= @item.genre.name %>
  <h4>税込価格(税抜価格)</h4>
  <%= @item.price %>
  <h4>販売ステータス</h4>
  <% if @item.is_active? %>
    <span>販売中</span>
  <% else %>
    <span>販売停止中</span>
  <% end %>
  <p><%= link_to '編集する',edit_admin_item_path %></p>
</div>